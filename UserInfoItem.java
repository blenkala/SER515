import java.io.*;

public class UserInfoItem {
      public void getUserInfoItem(String uname) throws IOException {
            ProductIterator pIterObject=new ProductIterator();
            pIterObject.hasNext(uname);
      }
}
